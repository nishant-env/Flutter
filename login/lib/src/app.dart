import 'package:flutter/material.dart';
import 'login.dart';
class App extends StatelessWidget
{
  build(context)
  {
    return Scaffold(
        appBar: AppBar(
         title:Text('Nishant'),
        centerTitle: true,
        ),
      body: Text('Hello'),  
      );
      
  }
}
